name: Build LineageOS OPlus SM8250 Kernel with KernelSU and cpufake support

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: '内核版本标签'
        required: false
        default: 'v1.0'

env:
  TZ: Asia/Shanghai
  CLANG_VERSION: clang-r547379
  Lineage_VERSION: lineage-23.2

jobs:
  build:
    name: Build Kernel with KernelSU (rsuntk)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Set swap to 10G
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 10

    - name: Setup build kernel environment
      run: |
        echo "BUILD_TIME=$(TZ=Asia/Shanghai date "+%y%m%d")" >> $GITHUB_ENV
        echo "BUILD_TIME_1=$(TZ=Asia/Shanghai date "+%Y-%m-%d")" >> $GITHUB_ENV
        sudo apt-get update
        sudo apt-get install -y git ccache automake flex lzop bison gperf build-essential zip curl zlib1g-dev g++-multilib bzip2 libbz2-dev liblz4-tool make squashfs-tools dpkg-dev libssl-dev python3 bc libc6-dev-i386 libncurses5-dev
        mkdir -p $GITHUB_WORKSPACE/kernel_workspace

    - name: Cache Clang toolchain
      id: cache-clang
      uses: actions/cache@v5
      with:
        path: ${{ github.workspace }}/kernel_workspace/clang-aosp
        key: ${{ env.CLANG_VERSION }}

    - name: Download & extract Clang-aosp (if cache miss)
      if: steps.cache-clang.outputs.cache-hit != 'true'
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        mkdir -p clang-aosp
        wget -O ${CLANG_VERSION}.tar.gz \
          https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${CLANG_VERSION}.tar.gz
        tar -C clang-aosp -zxvf ${CLANG_VERSION}.tar.gz

    - name: Download kernel source
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        git clone --recursive https://github.com/LineageOS/android_kernel_oneplus_sm8250 -b ${{ env.Lineage_VERSION }} android-kernel --depth=1

    - name: Set up KernelSU (rsuntk)
      run: |
        cd kernel_workspace/android-kernel

        curl -LSs https://raw.githubusercontent.com/rsuntk/KernelSU/main/kernel/setup.sh | bash -s main
        bash $GITHUB_WORKSPACE/patches/kernel-4.19-patch.sh || true

        # KernelSU 配置
        echo "CONFIG_KSU_MANUAL_HOOK=y" >> arch/arm64/configs/vendor/kona-perf_defconfig

        # cpufake 模块必需配置
        echo "# cpufake module support" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_MODULES=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_MODULE_UNLOAD=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_MODVERSIONS=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "# CONFIG_MODULE_SIG is not set" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "# CONFIG_MODULE_SIG_FORCE is not set" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "# CONFIG_MODULE_SIG_ALL is not set" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_KALLSYMS=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_KALLSYMS_ALL=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_KPROBES=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_KPROBE_EVENTS=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        echo "CONFIG_KRETPROBES=y" >> arch/arm64/configs/vendor/kona-perf_defconfig

        RSKSUVER=$(cd KernelSU && expr $(git rev-list --count HEAD) + 29971)
        echo "RSKSUVER=$RSKSUVER" >> $GITHUB_ENV

        sed -i 's/ -dirty//g' scripts/setlocalversion

    - name: Build kernel
      run: |
        cd kernel_workspace/android-kernel
        export ARCH=arm64
        export SUBARCH=arm64
        export BRAND_SHOW_FLAG=oneplus
        export PATH=$GITHUB_WORKSPACE/kernel_workspace/clang-aosp/bin:$PATH
        export KBUILD_BUILD_HOST=Github-Action
        export KBUILD_BUILD_USER=$(echo ${{ github.actor }} | tr A-Z a-z)

        BA_CMD="CLANG_TRIPLE=aarch64-linux-gnu-"
        EX_CMD="LD=ld.lld LLVM=1"
        DEFCONFIG="vendor/kona-perf_defconfig vendor/oplus.config"

        make O=out CC=clang $BA_CMD $EX_CMD $DEFCONFIG
        make -j$(nproc --all) O=out CC=clang $BA_CMD $EX_CMD

    - name: Check kernel output
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/android-kernel/out

        if [ -f arch/arm64/boot/Image ]; then
          echo "CHECK_IMAGE=true" >> $GITHUB_ENV
          echo "KERNEL_VERSION=$(cat include/config/kernel.release)" >> $GITHUB_ENV
        else
          echo "Kernel output file is empty"
          exit 1
        fi

    - name: Verify cpufake support in kernel config
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/android-kernel/out
        echo "=== 验证 cpufake 模块支持配置 ==="
        grep -E "CONFIG_MODULES=|CONFIG_MODULE_SIG|CONFIG_KALLSYMS|CONFIG_KPROBES" .config || true

    - name: Prepare AnyKernel3 and package
      run: |
        cd kernel_workspace
        git clone --depth=1 https://github.com/osm0sis/AnyKernel3
        sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
        sed -i 's!BLOCK=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;!BLOCK=auto;!g' AnyKernel3/anykernel.sh
        sed -i 's/IS_SLOT_DEVICE=0;/IS_SLOT_DEVICE=auto;/g' AnyKernel3/anykernel.sh

        # 更新内核描述
        sed -i 's/kernel.string=.*/kernel.string=OnePlus 8 Pro KernelSU + cpufake support by ${{ github.actor }}/' AnyKernel3/anykernel.sh

        rm -rf AnyKernel3/.git* AnyKernel3/README.md
        cp android-kernel/out/arch/arm64/boot/Image AnyKernel3/

        cd AnyKernel3
        zip -r AK3-${{ env.Lineage_VERSION }}-OPlus-SM8250-KSU-cpufake-${{ github.event.inputs.kernel_version }}.zip ./*

    - name: Upload Image artifact
      if: env.CHECK_IMAGE == 'true'
      uses: actions/upload-artifact@v6
      with:
        name: Image-KSU-cpufake
        path: kernel_workspace/android-kernel/out/arch/arm64/boot/Image

    - name: Upload AnyKernel3 ZIP
      uses: actions/upload-artifact@v6
      with:
        name: AK3-KSU-cpufake-${{ github.event.inputs.kernel_version }}
        path: ${{ github.workspace }}/kernel_workspace/AnyKernel3/AK3-*.zip

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: kernel-${{ github.event.inputs.kernel_version }}
        name: OnePlus 8 Pro Kernel ${{ github.event.inputs.kernel_version }}
        body: |
          ## OnePlus 8 Pro 内核 - KernelSU + cpufake 模块支持

          ### 版本信息
          - 设备: OnePlus 8 Pro (instantnoodlep) / OnePlus 8 / OnePlus 8T / OnePlus 9R
          - 基于: LineageOS ${{ env.Lineage_VERSION }} 内核源码
          - 内核版本: ${{ env.KERNEL_VERSION }}
          - KernelSU: rsuntk 版本 ${{ env.RSKSUVER }}
          - 编译时间: ${{ env.BUILD_TIME_1 }}

          ### 功能特性
          - 集成 KernelSU (rsuntk 版本)
          - 禁用 MODULE_SIG_FORCE (支持未签名模块)
          - 启用完整 KALLSYMS 符号表
          - 启用 KPROBES 完整功能
          - 支持 cpufake.ko 模块加载

          ### 刷入方法
          1. 重启到 Recovery (TWRP/OrangeFox)
          2. 刷入 AK3-*.zip
          3. 重启系统
          4. 安装 KernelSU Manager APK (rsuntk 版本)
          5. 使用 insmod 加载 cpufake.ko 模块

          ### 验证方法
          ```bash
          # 检查内核版本
          adb shell uname -r

          # 检查 KernelSU
          adb shell su -c "ksud -V"

          # 检查模块签名配置
          adb shell su -c "zcat /proc/config.gz | grep MODULE_SIG"

          # 加载 cpufake 模块
          adb shell su -c "insmod /path/to/cpufake.ko"
          adb shell lsmod | grep cpufake
          ```

          ### 注意事项
          - 刷入前务必备份当前 boot 分区
          - 支持 A/B 分区设备，自动识别槽位
          - 适用于 OnePlus 8 系列和 OnePlus 9R
          - KernelSU Manager 下载：https://t.me/rsukrnlsu
        files: |
          kernel_workspace/AnyKernel3/AK3-*.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Save build info
      run: |
        cd kernel_workspace
        {
          echo "RSKSUVER=${{ env.RSKSUVER }}"
          echo "KERNEL_VERSION=${{ env.KERNEL_VERSION }}"
          echo "BUILD_TIME_1=${{ env.BUILD_TIME_1 }}"
          echo "cpufake_support=enabled"
        } > build-info.txt

    - name: Upload build info
      uses: actions/upload-artifact@v6
      with:
        name: build-info
        path: kernel_workspace/build-info.txt
